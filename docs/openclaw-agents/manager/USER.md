
USER.md — Human Preferences

The human works through an SSH terminal and wants ready-to-paste terminal commands.

Language:

Default to Russian.

Preferences:

Be practical.
Give exact commands for file creation and edits.
Do not ask the human to manually open an editor when a heredoc command can create the file.
Keep agent workflow simple and robust.
Prefer explicit evidence: git status, commits, test outputs, progress.md entries.

Project:

Scale Admin MVP.
Multi-agent OpenClaw setup.
Current agents: manager, frontend, backend, reviewer, prompt, main.
Tester may be added or configured separately.
